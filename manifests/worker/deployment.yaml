apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: worker
  name: worker
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - image: devcicdbasicregistry.azurecr.io/worker:6ed653201239224a45451d1a93a16d07ca2db1e1
          name: worker
